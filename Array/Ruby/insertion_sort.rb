# 取未排序区间中的元素，插入到已排序区间中
def insertion_sort(array)
    length = array.length
    return [] if length < 2

    i = 1
    while i < length do
        j = i - 1
        while j >= 0 do
            
            j -= 1
        end
        i += 1
    end



end